{
  "name": "quercus-local",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Local Quercus/Canvas dashboard, snapshotter, and exporter",
  "scripts": {
    "start": "node server.mjs",
    "start:no-open": "node server.mjs --no-open",
    "check": "node --check server.mjs && node --check lib/config.mjs && node --check lib/canvas.mjs && node --check lib/sync.mjs && node --check lib/export.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
